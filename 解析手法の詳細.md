# ArduPilot System Identification 解析手法の詳細

## 概要

本解析ツールは、ArduPilotのSystem Identification（SYSID）モードで取得したログデータから、制御系の周波数応答特性を推定し、ボード線図、コヒーレンス関数、ステップ応答を算出する。本文書では、使用している信号処理手法および制御理論に基づく解析方法を数式とともに詳述する。

---

## 1. データ取得と前処理

### 1.1 入出力信号の定義

System Identification実験では、制御系に既知の励振信号を入力し、その応答を観測することでシステムの動特性を同定する。

- **入力信号** $u(t)$: レートコントローラの出力（例: `RATE.ROut`, `RATE.POut`, `RATE.YOut`）
- **出力信号** $y(t)$: 推定された角速度応答（例: `SIDD.Gx`, `SIDD.Gy`, `SIDD.Gz`）

ここで、$t$ は時刻（秒）を表す。

### 1.2 時間同期と補間

入力信号と出力信号は異なるサンプリングレートで記録される可能性があるため、線形補間により共通の時間軸に揃える。

共通時間軸 $t_{\text{common}}$ を定義し、

$$
u_{\text{interp}}(t_{\text{common}}) = \text{interp}(u(t), t)
$$

$$
y_{\text{interp}}(t_{\text{common}}) = \text{interp}(y(t), t)
$$

サンプリング周期 $\Delta t$ から、サンプリング周波数は

$$
f_s = \frac{1}{\Delta t}
$$

---

## 2. パワースペクトル密度とクロススペクトル密度の推定

### 2.1 Welch法の概要

Welch法は、信号を重複するセグメントに分割し、各セグメントに窓関数を適用してフーリエ変換を行い、その平均を取ることでスペクトル推定の分散を低減する手法である。

### 2.2 パワースペクトル密度（PSD）

入力信号 $u(t)$ のパワースペクトル密度 $P_{uu}(f)$ は、

$$
P_{uu}(f) = \lim_{T \to \infty} \frac{1}{T} \mathbb{E}\left[ |U(f)|^2 \right]
$$

ここで、$U(f)$ は $u(t)$ のフーリエ変換、$\mathbb{E}[\cdot]$ は期待値演算子、$T$ は観測時間である。

同様に、出力信号 $y(t)$ のパワースペクトル密度 $P_{yy}(f)$ を計算する。

### 2.3 クロススペクトル密度（CSD）

入力信号 $u(t)$ と出力信号 $y(t)$ のクロススペクトル密度 $P_{uy}(f)$ は、

$$
P_{uy}(f) = \lim_{T \to \infty} \frac{1}{T} \mathbb{E}\left[ U^*(f) Y(f) \right]
$$

ここで、$U^*(f)$ は $U(f)$ の複素共役、$Y(f)$ は $y(t)$ のフーリエ変換である。

実装では、scipy.signal.welch および scipy.signal.csd を使用してこれらを計算する。

---

## 3. 周波数応答関数（FRF）の算出

### 3.1 伝達関数の推定

線形時不変（LTI）システムの周波数領域表現において、入出力関係は次式で表される。

$$
Y(f) = H(f) \cdot U(f)
$$

ここで、$H(f)$ は周波数応答関数（Frequency Response Function, FRF）である。

クロススペクトル密度と入力のパワースペクトル密度から、$H(f)$ は次のように推定される。

$$
H(f) = \frac{P_{uy}(f)}{P_{uu}(f)}
$$

この推定は、H1推定量として知られている。

### 3.2 ゲインと位相の計算

周波数応答関数 $H(f)$ を極形式で表すと、

$$
H(f) = |H(f)| e^{j\phi(f)}
$$

ここで、

- **ゲイン（振幅）**: $|H(f)|$
- **位相**: $\phi(f) = \arg(H(f))$

デシベル表記では、

$$
|H(f)|_{\text{dB}} = 20 \log_{10} |H(f)|
$$

位相は度数法で、

$$
\phi(f)_{\text{deg}} = \frac{180}{\pi} \phi(f)
$$

---

## 4. コヒーレンス関数

### 4.1 定義

コヒーレンス関数 $\gamma^2(f)$ は、入力と出力の間の線形相関の度合いを表す指標であり、0から1の値を取る。

$$
\gamma^2(f) = \frac{|P_{uy}(f)|^2}{P_{uu}(f) \cdot P_{yy}(f)}
$$

### 4.2 解釈

- $\gamma^2(f) \approx 1$: 入力と出力が強く線形相関している（測定品質が高い）
- $\gamma^2(f) < 0.9$: ノイズの影響、非線形性、または外乱の存在を示唆

コヒーレンスが低い周波数帯域では、推定された周波数応答の信頼性が低い。

---

## 5. ボード線図

### 5.1 構成

ボード線図（Bode diagram）は、周波数応答を2つのプロットで表現する。

1. **ゲイン線図**: 周波数 $f$ に対する $|H(f)|_{\text{dB}}$ のプロット（対数スケール）
2. **位相線図**: 周波数 $f$ に対する $\phi(f)_{\text{deg}}$ のプロット（対数スケール）

### 5.2 特徴抽出

ボード線図から以下の特性を読み取ることができる。

- **DCゲイン**: $|H(f)|$ の $f \to 0$ での値
- **カットオフ周波数**: ゲインが $-3\,\text{dB}$ 減衰する周波数
- **バンド幅**: システムが応答できる周波数範囲

---

## 6. 安定性マージンの計算

### 6.1 ゲイン交差周波数とゲイン余裕

**ゲイン交差周波数** $f_c$ は、ゲインが $0\,\text{dB}$ となる周波数である。

$$
|H(f_c)|_{\text{dB}} = 0
$$

このとき、**位相余裕（Phase Margin, PM）** は、

$$
\text{PM} = 180° + \phi(f_c)
$$

位相余裕は、システムの安定性の指標であり、一般に $\text{PM} > 45°$ が望ましい。

### 6.2 位相交差周波数とゲイン余裕

**位相交差周波数** $f_{\phi}$ は、位相が $-180°$ となる周波数である。

$$
\phi(f_{\phi}) = -180°
$$

このとき、**ゲイン余裕（Gain Margin, GM）** は、

$$
\text{GM} = -|H(f_{\phi})|_{\text{dB}}
$$

ゲイン余裕は、一般に $\text{GM} > 6\,\text{dB}$ が望ましい。

### 6.3 安定性評価基準

本ツールでは、以下の基準で安定性を評価する。

- **非常に良好**: $\text{PM} > 45°$ かつ $\text{GM} > 8\,\text{dB}$
- **良好**: $\text{PM} > 30°$ かつ $\text{GM} > 6\,\text{dB}$
- **限界安定**: $\text{PM} > 0°$ かつ $\text{GM} > 0\,\text{dB}$
- **危険（不安定）**: 上記以外

---

## 7. ステップ応答の推定

### 7.1 モデルフィッティング

周波数応答から時間領域のステップ応答を推定するため、1次遅れシステムでモデル化する。

$$
G(s) = \frac{K}{\tau s + 1}
$$

ここで、

- $K$: DCゲイン（定常ゲイン）
- $\tau$: 時定数
- $s$: ラプラス変数

### 7.2 パラメータ推定

**DCゲイン** $K$ は、低周波数帯域でのゲイン値から推定する。

$$
K = |H(f)|_{f \to 0}
$$

**カットオフ周波数** $f_{-3\text{dB}}$ は、ゲインが $K/\sqrt{2}$ となる周波数である。

$$
|H(f_{-3\text{dB}})| = \frac{K}{\sqrt{2}}
$$

時定数 $\tau$ は、カットオフ角周波数 $\omega_c = 2\pi f_{-3\text{dB}}$ から、

$$
\tau = \frac{1}{\omega_c}
$$

### 7.3 正規化ステップ応答の計算

単位ステップ入力 $u(t) = 1 \cdot \mathbf{1}(t)$ に対する応答は、ラプラス変換により、

$$
Y(s) = G(s) \cdot \frac{1}{s} = \frac{K}{\tau s + 1} \cdot \frac{1}{s}
$$

逆ラプラス変換を適用すると、

$$
y(t) = K \left(1 - e^{-t/\tau}\right)
$$

**正規化**のため、$K$ で除すと、

$$
y_{\text{norm}}(t) = 1 - e^{-t/\tau}
$$

これにより、最終値が 1 に収束する正規化ステップ応答が得られる。

### 7.4 立ち上がり時間と整定時間

- **立ち上がり時間** $t_r$（10%から90%に達する時間）:

$$
t_r \approx 2.2 \tau
$$

- **整定時間** $t_s$（最終値の2%以内に収まる時間）:

$$
t_s \approx 4 \tau
$$

---

## 8. 実装における数値計算手法

### 8.1 Welch法のパラメータ

- **セグメント長** `nperseg`: データ長の 1/8 または最大 4096 サンプル
- **オーバーラップ**: デフォルト 50%（scipy.signal.welch の既定値）
- **窓関数**: Hanning窓（scipy.signal.welch の既定値）

### 8.2 周波数範囲のフィルタリング

解析対象の周波数範囲 $[f_{\min}, f_{\max}]$ に対して、該当する周波数ビンのみを抽出する。

$$
f_{\min} \leq f \leq f_{\max}
$$

本ツールのデフォルトは $f_{\min} = 0.5\,\text{Hz}$、$f_{\max} = 100\,\text{Hz}$ である。

### 8.3 数値安定性の考慮

ゼロ除算を防ぐため、パワースペクトル密度に微小値（例: $10^{-10}$）を加算する。

$$
H(f) = \frac{P_{uy}(f)}{P_{uu}(f) + \epsilon}
$$

ここで、$\epsilon = 10^{-10}$ である。

---

## 9. 結果の検証と品質評価

### 9.1 コヒーレンスによる品質評価

コヒーレンスが低い周波数では、推定精度が低い可能性がある。一般に、$\gamma^2(f) > 0.9$ の周波数帯域を信頼できるデータとみなす。

### 9.2 時間領域での検証

推定した伝達関数から計算したステップ応答が、物理的に妥当な挙動（オーバーシュート、整定時間など）を示すかを確認する。

### 9.3 再現性の確認

複数回の実験データで同様の周波数応答が得られることを確認することで、推定の信頼性が向上する。

---

## 10. 理論的根拠と参考文献

本ツールで使用している手法は、以下の理論と標準に基づいている。

### 10.1 Welch法

- Welch, P. D. (1967). "The use of fast Fourier transform for the estimation of power spectra: A method based on time averaging over short, modified periodograms." IEEE Transactions on Audio and Electroacoustics, 15(2), 70-73.

### 10.2 周波数応答関数推定

- Bendat, J. S., & Piersol, A. G. (2010). "Random Data: Analysis and Measurement Procedures" (4th ed.). Wiley.

### 10.3 制御系の周波数領域解析

- Ogata, K. (2010). "Modern Control Engineering" (5th ed.). Prentice Hall.
- Franklin, G. F., Powell, J. D., & Emami-Naeini, A. (2014). "Feedback Control of Dynamic Systems" (7th ed.). Pearson.

### 10.4 ArduPilot System Identification

- ArduPilot Documentation: https://ardupilot.org/copter/docs/systemid-mode-operation.html
- pyAircraftIden: https://github.com/xuhao1/pyAircraftIden

---

## 11. 数式記号表

| 記号 | 説明 |
|------|------|
| $u(t)$ | 入力信号（時間領域） |
| $y(t)$ | 出力信号（時間領域） |
| $U(f)$ | 入力信号のフーリエ変換 |
| $Y(f)$ | 出力信号のフーリエ変換 |
| $H(f)$ | 周波数応答関数（FRF） |
| $P_{uu}(f)$ | 入力のパワースペクトル密度 |
| $P_{yy}(f)$ | 出力のパワースペクトル密度 |
| $P_{uy}(f)$ | クロススペクトル密度 |
| $\gamma^2(f)$ | コヒーレンス関数 |
| $\|H(f)\|$ | ゲイン（振幅） |
| $\phi(f)$ | 位相 |
| $K$ | DCゲイン（定常ゲイン） |
| $\tau$ | 時定数 |
| $f_c$ | ゲイン交差周波数 |
| $f_{\phi}$ | 位相交差周波数 |
| $\text{PM}$ | 位相余裕（Phase Margin） |
| $\text{GM}$ | ゲイン余裕（Gain Margin） |
| $G(s)$ | 伝達関数（ラプラス領域） |
| $s$ | ラプラス変数 |
| $f_s$ | サンプリング周波数 |

---

## 12. まとめ

本解析ツールは、ArduPilotのSystem Identificationモードで取得したログデータから、Welch法に基づくパワースペクトル推定とクロススペクトル推定を行い、周波数応答関数、ボード線図、コヒーレンス関数を算出する。さらに、推定した周波数応答から1次遅れシステムモデルをフィッティングし、正規化ステップ応答を計算する。安定性マージン（位相余裕・ゲイン余裕）の評価により、制御系の安定性を定量的に評価できる。

これらの手法は、制御工学および信号処理の標準的な理論に基づいており、ドローンやロボットの制御系設計・チューニングに広く応用されている。
